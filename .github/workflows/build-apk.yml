name: Build APK

on:
  workflow_dispatch:
  push:
    branches: [ main, master ]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    env:
      ANDROID_HOME: /usr/local/lib/android/sdk
      ANDROID_SDK_ROOT: /usr/local/lib/android/sdk

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            git zip unzip autoconf libtool pkg-config \
            zlib1g-dev libncurses-dev libtinfo6 cmake \
            libffi-dev libssl-dev

      - name: Install Android SDK build-tools and platform-tools
        run: |
          export PATH="$ANDROID_SDK_ROOT/cmdline-tools/latest/bin:$PATH"
          yes | sdkmanager --licenses || true
          TARGET_BUILD_TOOLS=34.0.0
          FALLBACK_BUILD_TOOLS=33.0.2
          if sdkmanager --install "build-tools;${TARGET_BUILD_TOOLS}"; then
            echo "Installed build-tools ${TARGET_BUILD_TOOLS}"
          else
            echo "Falling back to build-tools ${FALLBACK_BUILD_TOOLS}"
            sdkmanager --install "build-tools;${FALLBACK_BUILD_TOOLS}"
          fi
          sdkmanager --install "platform-tools"

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install buildozer cython==0.29.36  # Cython <3 required by Buildozer on Python 3.11; revisit when Buildozer supports Cython 3

      - name: Build debug APK
        working-directory: android_share_zip_mvp
        run: |
          buildozer android debug

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: sharetozip-apk
          path: android_share_zip_mvp/bin/*.apk
          if-no-files-found: error
